service:
  name: serverless-nest
app: serverless-nest

package:
  excludeDevDependencies: false
  exclude:
  - client/**

plugins:
  - serverless-offline

provider:
  name: aws
  runtime: nodejs14.x

functions:
  main:
    handler: api/dist/main.handler
    events:
      - httpApi: '*'